# Базовый URL бэкенда (без завершающего слэша). Используется в src/lib/api.ts и связанных вызовах.
VITE_API_URL=http://localhost:8000

# Фаза 13: включить попытки чтения/сохранения профиля через API.
# false = только локальные fallback-источники (моки/localStorage)
VITE_USE_PROFILE_API=false
