# Аудит модалок вакансий (фаза 5.1 детального плана)

**Дата:** март 2026.

## Сравнение с `frontend old/components/vacancies/`

| Файл | `frontend old` | `fe_migration/src/components/vacancies/` |
|------|----------------|------------------------------------------|
| `AddVacancyModal.tsx` | есть | есть |
| `VacancyEditModal.tsx` | есть | есть |

Других файлов `*Modal*.tsx` в этой директории **в обоих** проектах нет.

## Вывод

Отдельные модалки из старого дерева **не пропали**: набор совпадает. Отличия по размеру/SHA между версиями связаны с доработками UI и логики в `fe_migration`, а не с отсутствующими компонентами.

Дальнейшая работа по 5.1 — при необходимости **построчный** diff полей и поведения внутри `AddVacancyModal` / `VacancyEditModal`, а не поиск «потерянных» файлов.

## Уточнение (2026-03-18)

- `AddVacancyModal.tsx`: `frontend old` и `fe_migration` совпадают по содержимому (`diff -q` без отличий).
- `VacancyEditModal.tsx`: файл отличается, но критичные блоки модалки присутствуют в обоих версиях (история правок, восстановление версии, вкладка интеграций, блок просмотра связей и интеграций).
- На текущем этапе 5.1 считаем закрытым как **точечный хвост**: дополнительных обязательных правок по diff не требуется.
