# План миграции UPD — нерешённое и отложенное

**Назначение этого файла:** в **`MIGRATION_PLAN.md`** лежит методология (части A–K), фазы 0–15 и чеклист переноса страниц. Сюда вынесено всё, что **ещё не сделано**, **специально отложено на конец**, и то, о чём говорили в чате (smoke, расхождения с эталоном, отдельные улучшения).

**Единый мастер-план остатка:** **`Детальный план остаточной миграции hr_helper_fe.md`** (корень репозитория). Статус по фазам — **`fe_migration/docs/DETAILED_PLAN_STATUS.md`**.

**Два параллельных направления (как в мастер-плане):**

| Направление | Содержание |
|-------------|------------|
| **Приёмка** | **Фаза 14** детального плана + **§1–3 ниже** — smoke по **`fe_migration/docs/MIGRATION_CHECK_URLS.md`**, порядок ручного приоритета: **workflow → wiki → company-settings → ATS → профиль**, затем остальное; **`npm run build`** зелёный. |
| **Крупная разработка** | **Фазы 8, 12, 13** — отдельными задачами; бенчмарки (**6.2** детального плана) **закрыты** на UI + моках; перед релизом всё равно нужен **smoke (фаза 14)**. |

**Связанные документы:**

| Документ | Зачем |
|----------|--------|
| **`Детальный план остаточной миграции hr_helper_fe.md`** | Мастер-план: фазы 1–15, таблица объёмных фич |
| `MIGRATION_PLAN.md` | Методология переноса, чеклист H, части I–K |
| `fe_migration/docs/MIGRATION_CHECK_URLS.md` | Список URL для ручной проверки на **3002** (фаза 14) |
| `fe_migration/docs/MIGRATION_DIVERGENCES.md` | **Намеренные** и **обнаруженные** отличия от **3001** / `frontend old` — сюда писать, **не «тихо» выравнивая** код под эталон |
| `fe_migration/docs/UI_MIGRATION_PLAN.md` | Профиль, футер, FloatingActions — сверка с **frontend :3000** и рекомендации |
| **`fe_migration/docs/PAGES_AND_COMPONENTS_DOCS.md`** | Документирование страниц и компонентов (**фаза 15** детального плана) |
| **`fe_migration/docs/MOCK_SERVICES.md`** | Единый слой моков до API (**фаза 13**) |
| **`fe_migration/docs/MIGRATION_STATE_ARCHITECTURE_2026-03-18.md`** | Зафиксированное текущее состояние, архитектура и полнота migration before phase 15 |
| `FRONTENDS_STACK_AND_STRUCTURE.md` (корень репо) | Порты и три приложения |

---

## 0. Коротко: о чём был разговор про «расхождения»

- **Перенос по маршрутам** в основном уже есть (чеклист H в основном плане отмечен).
- **Полное совпадение с 3001** по пикселям и логике **не гарантировано**: часть экранов **намеренно** изменена (workflow, вики, профиль под 3000, скролл в AppLayout и т.д.) — это перечислено в **`fe_migration/docs/MIGRATION_DIVERGENCES.md`**.
- При приёмке: если видите отличие — **сначала** смотреть, есть ли оно в DIVERGENCES; если нет — **дописать в §9 журнала** там же. Исправлять «как на 3001» — **только по отдельной задаче**, если так решит продукт.

### Жёсткие исключения из плана

- [x] `Sidebar` — исключён из текущего плана миграции.
- [x] `Header` — исключён из текущего плана миграции.
- [x] `Footer` — исключён из текущего плана миграции.
- [x] `FloatingActions` (панель быстрых кнопок) — исключён из текущего плана миграции; позиция зафиксирована `left: 12px`.

---

## 1. Фаза 14 — smoke и финальная приёмка (детальный план; в `MIGRATION_PLAN` — часть F)

**Фаза 15** в детальном плане — это **документирование** (`PAGES_AND_COMPONENTS_DOCS.md`); **smoke и build** относятся к **фазе 14**.

Статус: **ручная работа**, чеклисты ниже не закрыты автоматически.

- [ ] Smoke на **3002** по **`fe_migration/docs/MIGRATION_CHECK_URLS.md`** (включая заглушки модулей); структура меню — **`fe_migration/docs/sidebar.md`**. Сверка каждого пункта с **3001** не обязательна, если нет задачи на пиксельный паритет.
- [ ] Каждый URL из smoke-листа открыть на 3002 — отметить в трекере / §9 DIVERGENCES при расхождениях.
- [ ] Смоук: логин (если используется) → главная → несколько разделов → выход.
- [ ] **Приоритет ручной проверки** (как в детальном плане и §3 ниже): **workflow → wiki → company-settings → ATS → профиль**, затем остальное из списка URL.
- [ ] Консоль на 3002: нет необработанных ошибок при типичных действиях.
- [ ] `npm run build` в `fe_migration` без ошибок (поддерживать зелёным при каждом релизе).

---

## 2. Таблица учёта (часть G) и цикл K

- [ ] Завести/заполнить таблицу: **Путь** | **Перенесено** | **I (строки)** | **I2 (импорты)** | **J1–J5** | **Build** | **Заметки**.
- [ ] Для критичных страниц пройти **полный цикл K** (A1 → I → J → галочка H уже стоит, но **качество** по I/J — отдельно).

---

## 3. Построчный аудит и функциональная приёмка (части I и J)

Чеклист H в основном плане говорит «файл перенесён», но **I и J** для многих страниц могли **не выполняться полностью**.

- [ ] **I1–I2:** дифф `*Page.tsx` ↔ `frontend old/.../page.tsx`, цепочка импортов без `next/*`.
- [ ] **J1–J5:** визуал, темы, мобильная ширина, ссылки, интерактив, граничные случаи, консоль, **Network** при подключённом API.
- [ ] Приоритет вручную: **workflow**, **вики (просмотр + редактирование)**, **company-settings**, **ATS**, **профиль**.

---

## 4. Часть D — довести инфраструктурные хвосты

- [ ] Аудит **`NEXT_PUBLIC_*` → `VITE_*`** по всему `fe_migration` (не только `api.ts`).
- [ ] Где в Next был **`metadata` / title** — при необходимости **`document.title`** в страницах Vite (часть D3 основного плана).
- [ ] **D2:** страницы с бывшим server-side `fetch` — убедиться, что клиентское поведение приемлемо (или завести задачу на React Query).

---

## 5. Часть L — документирование UI/логики страниц (**фаза 15** детального плана)

**Цель:** чтобы новый разработчик понимал блоки, скролл, действия, зависимости.

**Индекс и правила:** **`fe_migration/docs/PAGES_AND_COMPONENTS_DOCS.md`** (формат, шаблон, приоритеты, связь с `COMPONENTS_DOCUMENTATION.md` в модулях).

- [ ] Создать при необходимости каталог **`fe_migration/docs/pages/`** и завести карточки для приоритетных маршрутов (workflow, wiki, company-settings, ATS, профиль — первыми).
- [ ] Для каждой карточки: маршрут, `*Page.tsx`, ключевые компоненты, источник данных (мок / API), ссылки на модульную документацию.
- [ ] JSDoc на публичные хуки/утилиты фич — по желанию, не дублировать простыню из карточки страницы.
- [ ] Пройти чеклист L по странице (UI, вёрстка, функциональность, зависимости, логика) хотя бы для **ключевых** маршрутов из **`MIGRATION_CHECK_URLS.md`**.

---

## 6. Блок «Не завершённые и незавершённые улучшения» (из конца `MIGRATION_PLAN.md`)

> **Не путать с нумерацией детального плана:** здесь пункты **6.1–6.8** — локальная нумерация журнала `MIGRATION_PLAN_UPD`. Бенчмарки в детальном плане — **§6.1 / §6.2** (там **6.2 закрыт** на моках).

Перенесено сюда как **явная очередь**:

| # | Область | Задача |
|---|---------|--------|
| 6.1 | **Шрифты** | Довести типографику по проекту (согласовать с дизайном / эталоном). |
| 6.2 | **Вакансии `/vacancies`** | **Закрыто (2026-03-18):** модалки проверены по diff; `AddVacancyModal` идентичен old, `VacancyEditModal` оставлен с целевыми доработками. |
| 6.3 | **Вакансии — статистика** | Клик по карточкам «Всего / Активных / Неактивных» должен фильтровать список. **Сделано** на `VacanciesPage` — при регрессии проверить вручную. |
| 6.4 | **Вакансии — иконки** | **Закрыто для `components/vacancies/*` (2026-03-18):** в `VacancyEditModal` `⚠` заменён на `ExclamationTriangleIcon`; emoji вне `vacancies/` — отдельный scope. |
| 6.5 | **Бенчмарки** | Кнопка «Добавить бенчмарк» на `/finance/benchmarks`. **Закрыто** (март 2026); детальный план **§6.1 / 6.2**. |
| 6.6 | **Бенчмарки — контент** | Графики, интервалы, настройки сбора; согласование с **`/company-settings/finance`**. **Закрыто на моках** (март 2026); подключение к API — **фаза 13** + **`MOCK_SERVICES.md`**. |
| 6.7 | **Sidebar / меню** | **Исключено из плана.** Не трогать без отдельной задачи. |
| 6.8 | **Wiki-new** | Решение продукта: оставить как вариант сравнения с `/wiki` или мержить UX; маршруты уже есть (`/wiki-new/*`). |

---

## 7. Админка и роли доступа (из конца `MIGRATION_PLAN.md`)

- [x] Описать матрицу **роль → разделы/действия** (`fe_migration/docs/admin-roles.md`).
- [x] Реализовать базовые клиентские ограничения UI для ролей **admin**, **superadmin**, **csadmin**, **superuser** (guard + фильтрация пунктов).
- [x] Роль **user** — без доп. ограничений по этой модели.
- [ ] Недоступное в админке: защита на API (backend), клиентская часть уже есть.

### Следующий шаг по фазе 12 (без smoke)

- [x] Согласовать матрицу в `fe_migration/docs/admin-roles.md` для маршрутов `/admin`, `/admin/users`, `/admin/groups`.
- [x] Добавить клиентский guard-хук (`useRoleAccess`) и подключить в `AdminLayoutShell`.
- [x] Скрыть недоступные пункты/кнопки по матрице (без изменений в исключённых зонах shell).
- [x] Подключить роль из API профиля вместо `localStorage` (API-first, fallback на localStorage).

---

## 8. Рекомендации из `fe_migration/docs/UI_MIGRATION_PLAN.md` (следующие шаги)

- [x] **QuickButtonsPage:** слушать нативный `storage` по ключу `quickButtons` (`QUICK_BUTTONS_KEY`) для обновления списка в другой вкладке; `FloatingActions` — перечитка списка только при изменении этого ключа (или полном сбросе `localStorage`).
- [x] **Фаза 13 — API вместо моков + единый слой моков (frontend-часть закрыта):** сервис-обёртки внедрены (profile/quick-buttons/social/integrations), `VITE_USE_PROFILE_API`, унифицированы коды ошибок профиля (`ProfileApiResult`).
- [x] **Тесты:** Vitest — `quickButtonsStorage`, вкладки профиля, `SocialLinksManager` (лимит 3 на платформу).
- [x] **FloatingActions (a11y/UX в этой очереди):** исключено из плана вместе с самим компонентом.
- [ ] **Визуальный регресс:** 3000 vs 3002 (профиль, ATS; shell-элементы вне объёма исключений проверяем только на регресс «не сломалось»).
- [ ] **Документация:** `COMPONENTS_DOCUMENTATION.md` / `QuickButtonsPage.DOCUMENTATION.md` под `quickButtonsStorage` и FloatingActions.

### Следующий шаг по фазе 13 (без smoke)

- [x] Зафиксировать минимальные API-контракты для профильного слоя (стартовые endpoint'ы и флаг `VITE_USE_PROFILE_API`).
- [x] Создать сервисные функции-обёртки (mock/api switch) и подключить сначала к профилю.
- [x] Оставить `quickButtonsStorage` fallback при ошибках API.
- [x] Уточнить frontend-контракт endpoint'ов и коды ошибок для профильного блока (финальное backend-согласование — внешняя зависимость).

---

## 9. ATS и паритет с основным `frontend`

- [ ] Сверить экраны **fe_migration** (`Ats*Page`, `atsMocks.ts`) с **`frontend/src/features/ai/ats`** — полный паритет **не заявлен**; отличия заносить в **`fe_migration/docs/MIGRATION_DIVERGENCES.md` §9** или сюда в подпункты по мере решения.

---

## 10. Расширение `router-adapter` (часть E)

- [ ] При появлении страниц с **`useSearchParams`** и др. — добавить экспорт в `router-adapter.tsx`, если ещё нет.

---

## 11. Журнал расхождений при приёмке

- [ ] Вести **`fe_migration/docs/MIGRATION_DIVERGENCES.md`**, раздел **§9** — все новые находки при сравнении 3001 ↔ 3002.

---

## 12. Новые страницы в `frontend old`

- [ ] Если в Next появятся новые `app/**/page.tsx` — дописать в чеклист H основного плана и сюда задачу на перенос (цикл A1 + K).

## 15. Фаза 15 — документирование (без smoke)

- [ ] Создать `fe_migration/docs/pages/INDEX.md`.
- [ ] Заполнить 5 карточек в приоритете: workflow, wiki, company-settings, ATS, профиль.
- [ ] Для каждой карточки добавить ссылку на соответствующий `COMPONENTS_DOCUMENTATION.md`.

---

## 13. Очередь из обсуждения (не трогаем уже перенесённое из нового `frontend`)

| Зона | Статус / следующий шаг |
|------|-------------------------|
| **6.2** (детальный план) бенчмарки: дашборд, список `/all`, настройки | **Закрыто** на UI + моках (март 2026). API — **фаза 13**. |
| **2.2 / 6.7** Sidebar | Исключено из плана: без доработок в рамках миграции. |
| **Фаза 12** роли, скрытие UI | Черновик `fe_migration/docs/admin-roles.md`; реализация после согласования матрицы; API — отдельно. |
| **Фаза 13** API + единый слой моков | См. **`fe_migration/docs/MOCK_SERVICES.md`**; профиль и quick buttons — приоритет; бенчмарки/ATS — после контракта. |
| **3.4** `quickButtons` между вкладками | Закрыто: `storage` на `QUICK_BUTTONS_KEY` в `QuickButtonsPage`. |
| **5.1** модалки вакансий | Паритет с old не аудировался; при необходимости — отдельный чеклист по полям/вкладкам. |
| **ATS (март 2026)** | Зафиксировано в **`Детальный план…` §фаза 9**, **`fe_migration/docs/DETAILED_PLAN_STATUS.md`** (журнал правок), **`fe_migration/docs/ATS_MIGRATION_SCOPE.md`**: версии опыта, моки, переключатель Candidates. Паритет с `frontend/src/features/ai/ats` — по-прежнему §9 здесь. |

---

## 14. Журнал фиксации (документы ↔ код)

- **2026-03:** ATS — блок опыта/версий, моки кандидатов, стили табов слева; описано в детальном плане (доп. к фазе 9) и `DETAILED_PLAN_STATUS.md`.
- **2026-03:** Синхронизация планов: мастер — **`Детальный план остаточной миграции hr_helper_fe.md`**; **`MIGRATION_PLAN_UPD`** — §1 переименован в **фазу 14** (smoke); бенчмарки **6.2** детального плана — **готово**; **фаза 13** расширена единым слоем моков — **`fe_migration/docs/MOCK_SERVICES.md`**; пути к `DIVERGENCES` / `CHECK_URLS` унифицированы под `fe_migration/docs/`.
- **2026-03:** **Фаза 8** — автосверка `components/company-settings/` (`diff -rq`), документ **`fe_migration/docs/COMPANY_SETTINGS_PHASE8.md`**, **`MIGRATION_DIVERGENCES.md` §12** (в т.ч. упрощённые интеграции в `CompanySettingsPages.tsx`); ручной smoke по URL остаётся в очереди.
- **2026-03-18:** Единый **реестр документов** в **`Детальный план остаточной миграции hr_helper_fe.md`** (таблица в начале); **`FRONTENDS_STACK_AND_STRUCTURE.md`** — исправлены пути: `MIGRATION_PLAN.md` и `MIGRATION_PLAN_UPD.md` только в **корне репо**; **`MIGRATION_PLAN.md`** — таблица «Заметки по документации» расширена; **фаза 15** там согласована с мастер-планом (14 = smoke, 15 = docs); **`MIGRATION_CHECK_URLS.md`** — ссылки на мастер-план, `DETAILED_PLAN_STATUS`, `MIGRATION_PLAN_UPD` с корректным `../../`.
- **2026-03-18:** По решению команды из плана полностью исключены `Sidebar`, `Header`, `Footer`, `FloatingActions`; `FloatingActions` в коде сдвинут на `left: 12px` и зафиксирован как замороженный.
- **2026-03-18:** Закрыты хвосты вакансий: **5.1** (модалки проверены по diff, `AddVacancyModal` идентичен old) и **5.3** (в `VacancyEditModal` предупреждение переведено с emoji на `ExclamationTriangleIcon`); добавлены стартовые шаги по фазам **12/13/15** без smoke.

---

*Обновляй этот файл при закрытии пунктов: меняй `[ ]` на `[x]` или переноси выполненное в «архив» внизу при необходимости.*
